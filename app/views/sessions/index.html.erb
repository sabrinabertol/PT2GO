<div class="container">


  <h1 class="text-center m-5"> 🏋🏻‍♂️ Here are our Classes! 💪🏻🤸🏻‍♀️ </h1>



  <% if !current_user.nil? && current_user.pt %>
    <%= link_to new_session_path do %>

      <button type="button" class="btn btn-success center">Add your class!</button>
    <% end %>
  <% end %>


    <div class="d-flex justify-content-center flex-column p-3" >
         <% @sessions.each do |session| %>
         <%= link_to "", session_path(session), class: 'card-link' %>
          <div class="flip card_body">
            <% if session.photo.attached? %>
              <div class="front" style="background-image: url('<%= cl_image_path session.photo.key %>')">
                <h1 class="text-shadow"><%= session.name %></h1>
              </div>
           <% else %>
             <div class="front" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://res.cloudinary.com/dyndl1bic/image/upload/v1605194054/full-logo-03_qj0t4l.jpg)">
                <h1 class="text-shadow"><%= session.name %></h1>
             </div>
           <% end %>
           <div class="back">
             <p><%= session.location %></p>
             <p><%= session.date %></p>
             <p><%= session.description %></p>

          </div>
        <% end %>
    </div>


  <div id="map"
   style="width: 100%; height: 600px;"
   data-markers="<%= @markers.to_json %>"
   data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

</div>
